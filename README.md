# 📝 Checklist Champion

A clean, accessible, component-driven React app styled with a combination of TailwindCSS and Material UI.

## 🚀 Overview

This project is a lightweight to-do dashboard built in React (Functional Components + Hooks), styled with a combination of TailwindCSS for layout/spacing and Material UI for interactive widgets.

## 🌟 Features

- Core Functionality
- Add new tasks
- Mark tasks as completed
- Filter tasks by All / Pending / Completed
- View total and completed task counts
- Persist tasks in localStorage
- Smooth fade-style micro-animations
- Clean, responsive, minimal UI

## Architecture Highlights

- React Context + useReducer for global, predictable state management
- Separation of concerns (components, context, utils)
- Reusable UI components (ChecklistItem, Header, TaskInput, TodoList)
- Utility-first styling with TailwindCSS
- Material UI for accessible, consistent controls
- Pure utility functions for filtering, sanitization, and persistence

## 📦 Install & Run

```npm i```

```npm start```
